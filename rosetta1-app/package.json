{
  "name": "rosetta1-app",
  "version": "1.0.0",
  "description": "Rosetta1 autonomous trading bot — Tauri desktop shell",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:mac": "tauri build --target universal-apple-darwin"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
